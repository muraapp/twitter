<%= render 'form' %>

<p id="notice"><%= notice %></p>
<% @tweets.each do |tweet| %>
    <p><%= tweet.content %></p>
    <%= link_to "編集", edit_tweet_path(tweet.id) %>
    <%= link_to "削除", tweet_path(tweet), method: :delete, data: {confirm: '本当に削除してもいいですかね？'} %>
<% end %>